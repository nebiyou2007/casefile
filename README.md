# Casefile

Live zero-cost preview built from the uploaded Casefile project. Includes the core archive, search, category filters, case views, research-status notes, timelines, and source starting points.

The original server-side AI/search layer remains separate because it requires private API credentials and a server runtime.